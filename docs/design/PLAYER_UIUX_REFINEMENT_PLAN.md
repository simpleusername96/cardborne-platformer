---
type: plan
status: superseded
owner: BK
created: 2026-07-14
last_reviewed: 2026-07-14
topic: Production game UI and UX migration for contextual combat equipment, crafting, loot, replay, and save continuity
scope: Main menu, profiles, onboarding, preparation, forge, stage map, gameplay HUD, loot feedback, choices, settings, results, focus, and responsive validation
supersedes: ./PLAYER_FACING_FLOW.md
superseded_by: ../../.agent/execplans/2026-07-14-minimum-equipment-progression-vertical-slice.md
source: Owner feedback through 2026-07-14 and inspected production UI, input, field pickup, reward, save, and combat state
related:
  - ./COMBAT_EQUIPMENT_CRAFTING.md
  - ../product/2d_platform_action_card_game_prd.md
  - ../architecture/FIRST_SLICE_ARCHITECTURE.md
---

# 플레이어 UI/UX 개선 계획

> **Superseded 2026-07-14.** 이 문서의 프로필 3개, 제압/전술/정령술,
> 공명, 12개 도구 UI 목표는 실행하지 않는다. 현재 UI 범위와 실행 순서는
> `.agent/execplans/2026-07-14-minimum-equipment-progression-vertical-slice.md`가
> 소유한다. 이 문서는 이전 화면 조사와 비교 근거로만 유지한다.

## Purpose

현재의 기능 확인용 텍스트 중심 화면을 실제 게임처럼 읽히는 흐름으로 교체한다.
플레이어가 설명문을 읽지 않아도 다음 행동, 공격에 사용될 도구, 방어 가능 여부,
획득한 보상, 제작 결과, 저장 상태를 알 수 있어야 한다.

이 계획은 화면과 상호작용만 소유한다. 근접/원거리 자동 선택, 제작 비용,
액티브/패시브 분류, 정령석 효과, 상태, 원거리 자원 같은 게임 규칙은
`COMBAT_EQUIPMENT_CRAFTING.md`를 그대로 표시하며 UI에서 다시 계산하지 않는다.

## UX Direction

- **표면:** 2D 액션 게임의 플레이 HUD와 스테이지 사이 준비 화면.
- **주요 사용자 작업:** 싸우는 동안 즉시 판단하고, 안전 구역에서는 장비와 다음
  목표를 비교해 확정한다.
- **방향:** 차분한 폐허 탐험 분위기, 게임 플레이를 가리지 않는 얇은 HUD,
  실루엣과 짧은 동작 피드백 우선.
- **밀도:** 전투 화면은 낮음, 준비·제작 화면은 중간. 재료 전체 목록과 긴 설명은
  전투 화면에 상시 노출하지 않는다.
- **개선 방식:** 기존 생산 UI의 입력 리매핑, 보상 트랜잭션, 포커스 처리,
  반응형 재배치, 공통 스타일은 재사용한다. 클래스와 디버그 성격의 표현만 교체한다.
- **금지:** 마케팅 랜딩 페이지, 설명용 거대 텍스트, 중첩 카드, 작동하지 않는
  메뉴, raw ID, 화면을 덮는 반투명 패널, 색만으로 구분하는 상태.

## As-Is Review

| 영역 | 현재 상태 | 문제 |
| --- | --- | --- |
| 시작 흐름 | `CharacterSelect.gd`가 Warrior/Archer/Assassin 선택, 장비, 숙련을 한 화면에 결합. | 한 영웅과 세 전투 장비라는 목표와 충돌하고 첫 행동이 불명확함. |
| HUD | 체력, 클래스 상태, 코인과 4재료, 목표, 6개 행동 슬롯을 동시에 표시. | 전투 중 필요하지 않은 정보가 크고 자동 공격 도구·원거리 자원·방패 상태는 없음. |
| 공격 피드백 | 클래스별 공격 범위 표시는 존재하지만 실제 판정과 표현이 분리될 수 있음. | 어떤 장비가 나갈지, 어디까지 맞는지 공격 전에 확신하기 어려움. |
| 아이템 | 회복·보급·재료 필드 아이템과 짧은 수령 문구는 존재. 일반 재료는 같은 팔각형 형태를 공유. | 재료 종류가 색에 의존하고 지도 위의 실제 물건처럼 보이지 않음. |
| 적 보상 | 적 처치 보상은 필드 물체 없이 즉시 런 상태에 적용됨. | 플레이어가 무엇을 왜 얻었는지 공간적으로 연결되지 않음. |
| 상자 | `E` 상호작용과 보상 문구는 있으나 중요한 설계도 획득 연출과 비교가 없음. | 상자를 연 행동과 장비 성장의 의미가 약함. |
| 제작 | Rest & Forge와 장비 비교 기반은 있으나 클래스·기존 장비 모델에 묶여 있음. | 액티브/고유 특성, 재료 등급, 수리, 도구별 보급의 원인과 결과가 보이지 않음. |
| 저장 | 자동 프로필 저장과 백업은 있으나 플레이어가 고르는 슬롯과 유효한 Continue 흐름은 없음. | 저장 여부와 재개 위치를 신뢰하기 어려움. |
| 텍스트 | 영문 대문자, 개발 용어, 축약 재료명, 설명문이 화면 기능을 대신하는 곳이 있음. | 일반 게임 UI가 아니라 테스트 상태 보고처럼 보임. |

## To-Be Flow

```text
메인 메뉴
 -> 프로필 선택 또는 Continue
 -> 새 프로필: 무기 연습장 / 동일 보상으로 건너뛰기
 -> 장비 준비: 근접 / 원거리 / 방패 / 제압 / 전술 / 정령석 / 지원 장비
 -> 스테이지 지도: 첫 보상, 반복 보상, 놓친 설계도, 추천 준비 확인
 -> 스테이지 HUD
    -> 필드 아이템과 보상 수령
    -> 레벨/카드 선택
    -> 일시정지/설정
 -> 결과 요약
 -> 대장간: 제작 / 재제작 / 수리 / 원거리 보급
 -> 다음 스테이지 또는 이전 지역 재도전
 -> 보스
 -> 런 정산 / 프로필 저장
```

원칙:

- 한 화면의 주요 행동은 하나다. 장비 준비는 장착, 대장간은 제작/수리,
  스테이지 지도는 진입 결정에 집중한다.
- 모달 보상은 전투를 멈추고 중복 확정을 막는다.
- 화면 전환을 시작한 버튼은 진행 상태로 바뀌며 연속 입력을 받지 않는다.
- 취소 가능한 화면과 반드시 받아야 하는 보상을 명확히 구분한다.
- 생산 화면에 테스트, seed, route, validation, raw content ID를 노출하지 않는다.

## Information Architecture

### 메인 메뉴와 프로필

메인 메뉴 순서:

1. **계속하기:** 유효한 체크포인트 저장이 있을 때만 표시.
2. **새 게임:** 기존 중단 저장이 있으면 계속/포기 선택을 먼저 요구.
3. **프로필:** 로컬 슬롯 3개.
4. **연습장:** 완료 후에도 다시 입장 가능.
5. **설정.**
6. **종료.**

프로필 슬롯은 플레이 시간, 마지막 안전 지점, 완료한 최고 스테이지, 마지막
장비 구성을 보여준다. 비어 있는 슬롯은 `새 프로필`만 제공한다. 삭제는 슬롯
이름을 명시하고 길게 누르기 또는 두 번째 확인을 요구한다.

저장 상태는 `저장 중`, `저장됨`, `복구된 백업`, `저장 실패` 네 상태를 갖는다.
실패 시 플레이를 즉시 종료시키지 않고 다시 시도와 안전한 메뉴 복귀를 제공한다.

### 장비 준비

장비 준비 화면의 첫 화면에는 전투 도구, 액티브 기술, 지원 장비 세 묶음을
보여주되, 각 슬롯은 아이콘, 이름, 분류, 핵심 장단점 한 줄만 표시한다.

```text
전투 도구:  [근접 F] [원거리 F] [방패 G]
액티브:     [제압 Q] [전술 R] [정령술 V]
지원 장비:  [방어구] [장신구] [정령석] [소비 아이템]
```

- 도구 슬롯은 제작한 같은 역할 모델만, 기술 슬롯은 같은 책임의 해금 기술만
  목록에 나타난다. 정령술은 별도 선택하지 않고 정령석 상세에 묶여 있다.
- 도구 비교는 `액티브 행동`, `고유 특성`, `자원`, `강점`, `약점`을 먼저,
  `현재 -> 후보` 수치를 그다음에 보여준다.
- 액티브는 `ACTIVE`, 입력 없는 효과는 `PASSIVE` 표제를 한국어 명칭과 함께
  고정 위치에 표시한다. 재료 등급과 방어구 수치를 패시브라고 부르지 않는다.
- 잠긴 설계도는 획득 장소 힌트를 보여주되 장착 버튼처럼 보이지 않는다.
- 다음 스테이지의 적/위험 정보는 `원거리 적`, `중장갑`, `독성 지형`처럼 검증된
  압박 태그만 표시한다. 특정 장비를 정답이라고 강요하지 않는다.
- 장비 변경이 실제 저장에 반영되기 전 `준비 완료`에서 전체 구성을 검증한다.
- 한 화면 안에 카드 안의 카드를 만들지 않는다. 왼쪽 종류 목록, 가운데 장비
  목록, 오른쪽 비교/행동 영역의 세 열을 기본으로 한다.

### 대장간

상단 탭은 `제작`, `재제작`, `수리`, `보급` 네 개다. 탭은 동일한 선택 장비를
유지하고 각 작업에 필요한 정보만 바꾼다.

제작/재제작 상세:

- 도구 실루엣, 전투 역할, 액티브 행동, 고유 특성;
- 현재 재료 등급과 결과 등급;
- 도달/방어 범위, 입력 리듬, 표적/전달, 자원, 명시적 약점;
- 기본 설계도 값, 재료 기여, 최종 값;
- 보유/필요 재료를 `5 / 3`처럼 혼동하지 않고 `보유 5 · 필요 3`으로 표기;
- 제작 후 남는 수량;
- 장착 중인 장비와 달라지는 값;
- 확정 성공임을 전제로 한 하나의 `제작` 또는 `재제작` 명령.

수리 상세:

- 근접 무기와 방패 상태를 큰 수치와 막대로 표시;
- 무료 최소 정비가 적용되는 이유와 결과;
- 재료 1개당 복구량;
- 선택 수리와 모두 수리;
- 상태 0에서도 `파괴됨`이 아니라 `마모됨`이라고 표시.

보급 상세는 장착 원거리 도구에 따라 한 종류만 보여준다.

- 사냥 활: 현재/최대 화살, 무료 최소치, 화살 8발 제작 비용;
- 화승총: 현재/최대 탄약, 장전 여부, 무료 최소치, 탄약 4발 제작 비용;
- 회수 표창: 보유/배치/회수 중인 표창 3개와 안전 회수 명령;
- 뿌리 각인판: 집중 2회와 회복 시간. 구매·제작 버튼은 없음;
- 원소 탄약이나 여러 탄약을 고르는 하위 메뉴는 만들지 않는다.

### 스테이지 지도와 재도전

각 스테이지 항목은 다음을 구분한다.

- 진행 상태: 잠김, 첫 도전, 완료, 재도전 가능;
- 첫 완료 보상;
- 반복 시 최소 보장 재료와 주요 재료 계열;
- 놓친 고정 설계도, NPC 의뢰, 정령 시련;
- 마지막 체크포인트 저장 여부;
- 예상 플레이 시간과 주요 위험 태그.

확률 퍼센트만 나열하지 않는다. `금속 재료 최소 3`, `소형 방패 설계도 미획득`
처럼 결정에 필요한 결과를 보여준다. 기본 수리나 원거리 자원이 부족하다는 이유로
스테이지 버튼을 비활성화하지 않는다. 준비 화면으로 안내하고 안전장치를 적용한다.

## Gameplay HUD

### 기본 배치

| 위치 | 내용 | 표시 원칙 |
| --- | --- | --- |
| 좌상단 | 체력, 현재 소비 아이템 횟수 | 항상 표시, 작은 상태 설명 금지 |
| 상단 중앙 | 스테이지 목표/변경 | 변경 시 펼쳐지고 2-3초 뒤 한 줄로 축소 |
| 우상단 | 코인, 일시적 저장 상태 | 재료 전체 수량은 표시하지 않음 |
| 하단 중앙 | 상황 공격, 방어, 제압/전술/정령술, 소비 아이템 | 아이콘 + 현재 입력 + 재사용 상태 |
| 하단 좌측 | 장착 원거리 도구의 자원 하나, 근접 상태, 방패 상태 | 얇은 자원 묶음; 경고만 강조 |
| 보스전 상단 | 보스 이름, 체력, 단계 | 일반 목표를 대체, 패턴 설명문 금지 |

HUD는 배경을 넓게 덮는 패널 대신 아이콘, 얇은 막대, 짧은 배경 면을 사용한다.
게임 화면 중앙과 캐릭터 주변을 가리지 않는다.

### 상황 공격 미리보기

- 근접 행동이 선택되면 장착 도구 실루엣과 실제 도달/폭의 짧은 시작 표시를
  보여준다. 창·단검·망치가 모두 검 모양으로 보이지 않는다.
- 사냥 활은 조준선/궤적, 화승총은 직선 조준과 장전 상태, 투척날은 왕복 예상
  경로, 뿌리 각인판은 적 발밑의 지연 표식을 보여준다.
- 경계 거리에서 장비가 바뀔 때 아이콘은 100-150ms 안에 교차 전환하며 깜빡이지 않는다.
- 원거리 자원이 없거나 표적 정책이 실패하면 해당 실루엣은 나타나지 않고 가장
  가까운 원인 아이콘만 한 번 반응한다.
- 표적이 없으면 캐릭터 앞에 근접 공격의 방향만 보여준다.
- 범위 시각화와 실제 `Hitbox`/투사체 판정은 같은 데이터에서 만들어져야 한다.

### 방어 피드백

- 방패를 들면 캐릭터 앞 실제 방어 방식과 하단 방패 상태가 함께 반응한다.
- 정밀 방어 구간은 짧은 테두리 수축과 소리로 표시한다.
- 소형 방패는 정밀 구간 뒤 지속 방어 약화를, 대형 방패는 준비와 이동 고정을,
  거울 방패는 저장한 투사체 0-2개와 놓을 때 반사 방향을 별도로 보여준다.
- 방어 가능한 공격은 일반 경고, 강한 공격은 더 굵은 경고, 방어 불가 공격은
  모양이 끊어진 붉은 파열 표식으로 구분한다.
- 색을 보지 못해도 선 모양, 소리, 동작으로 세 종류를 구분할 수 있어야 한다.

### 액티브와 패시브

- `Q`는 제압, `R`은 전술, `V`는 장착 정령석 문양을 사용한다. 근접/원거리
  강화 기술처럼 장비 실루엣을 재사용하지 않는다.
- 제압·전술 재사용 대기시간은 숫자와 진행 표시를 함께 제공한다. `V`는 공명
  게이지가 가득 찼을 때만 강조한다.
- 사용 불가 이유는 가장 가까운 원인 하나만 보여준다: `유효한 지면 없음`,
  `재사용 대기 중`, `공명 부족`.
- 고유 특성, 장신구, 정령 동조, 런 카드는 별도 버튼 칸을 차지하지 않는다.
  충전되거나 발동할 때 관련 아이콘 옆에 1-2초만 표시한다.
- 영문 내부 ID나 긴 기술 설명은 HUD에 표시하지 않는다.

## World Items And Rewards

### 필드 아이템

| 종류 | 형태 | 획득 방식 | 피드백 |
| --- | --- | --- | --- |
| 회복 | 병/잎 실루엣 | 접촉 자동 획득, 최대 체력이면 남음 | 체력 막대 회복 + 짧은 수량 |
| 원거리 보급 | 화살/탄약/회수 신호/집중 문양 | 접촉 자동 획득 | 장착 도구 이름 + 실제 변화량 |
| 금속 | 각진 파편 | 짧은 튐 후 자석 획득 | 금속 아이콘 + 등급 + 수량 |
| 목재 | 막대 묶음 | 짧은 튐 후 자석 획득 | 목재 아이콘 + 등급 + 수량 |
| 섬유 | 감긴 실 | 짧은 튐 후 자석 획득 | 섬유 아이콘 + 등급 + 수량 |
| 가죽 | 접힌 조각 | 짧은 튐 후 자석 획득 | 가죽 아이콘 + 등급 + 수량 |
| 화약 | 봉한 주머니 | 짧은 튐 후 자석 획득 | 화약 아이콘 + 수량 + 탄약 용도 |
| 설계도 | 펼쳐진 도면 | `E` 확인 | 전체 보상 창, 새 행동과 제작 재료 표시 |
| 기술서 | 제압/전술 문양이 있는 책 | `E` 확인 | 책임, 액티브 행동, 제한 표시 |
| 정령석 | 고유 문양의 큰 석재 | `E` 확인 | 패시브 동조 1개, 액티브 정령술 1개 표시 |

일반 아이템 수령은 플레이를 멈추지 않는다. 설계도, 정령석, 보스 보상만 전투가
끝난 안전 상태에서 큰 보상 창을 사용한다.

### 수령 문구

- 일반 재료: `강철 조각 +2`처럼 한 줄, 1.5초 이내.
- 여러 재료: 같은 계열은 합치고 최대 세 줄. 넘으면 `외 2종`으로 축약하고
  결과 화면에서 전체를 보여준다.
- 설계도: 이름, 역할, 액티브 행동, 고유 특성, 약점, 제작 가능 여부.
- 상자: 상자 열림 자체가 아니라 실제 획득 결과를 제목으로 사용.
- 중복 또는 이미 정산된 보상은 조용히 사라지지 않고 저장 복구 상황에만
  `이미 획득한 보상` 한 줄을 제공한다.

## Choice And Result Screens

- 레벨/카드 선택은 현재 장비에 작동하는 선택지만 제공한다.
- 각 선택은 행동 변화 한 줄, 영향받는 장비 아이콘, 현재 값과 결과 값을 보여준다.
- 선택 전에는 적용하지 않고, 확정 후 새 스냅샷을 받은 다음 전투를 재개한다.
- 사망/완료 결과는 원인, 스테이지 진행, 얻은 설계도·정령석·재료, 장비 상태,
  저장 결과를 보여준다.
- 사망 화면은 `다시 시작`과 `대장간/준비`의 차이를 설명하지 않고 결과로
  구분한다. 중단 저장을 소비하는 행동은 별도 확인을 요구한다.

## Input, Focus, And Accessibility

- 모든 명령은 키보드와 표준 게임패드로 완료 가능해야 한다.
- 입력 아이콘은 마지막 사용 장치가 바뀌면 즉시 교체한다.
- 리매핑 시 충돌, 취소, 기본값 복원, 저장 실패 상태를 제공한다.
- 패드 포커스는 화면 진입 시 가장 안전한 주요 선택에 위치하고, 닫은 뒤 원래
  버튼으로 돌아간다.
- 포커스가 없는 버튼, 보이지 않는 스크롤 위치, 포커스 함정이 없어야 한다.
- 조작 대상은 최소 40px, 주요 확인은 44px 이상을 목표로 한다.
- 화면 흔들림, 피해 번쩍임, 정령 효과 강도, 자막/효과음 표시, 진동을 조절한다.
- 위험, 재료, 정령은 색 외에 형태와 문양으로 구분한다.
- 모션 감소 시 튐·자석 이동·패널 전환을 짧은 페이드로 대체한다.

## Responsive And State Matrix

검증 화면 크기:

- `960x540`: 최소 지원, 장비 준비는 2열 또는 탭식으로 재배치;
- `1280x720`: 기본 플레이·준비 기준;
- `1920x1080`: 화면 가장자리에 과도하게 벌어지지 않도록 최대 폭 제한.

모든 화면이 갖춰야 할 상태:

| 상태 | 요구사항 |
| --- | --- |
| 기본 | 주요 행동과 현재 선택이 즉시 보임. |
| 포커스/호버 | 배경색만이 아니라 테두리·실루엣 변화 제공. |
| 선택됨 | 현재 장착/선택과 단순 포커스를 구분. |
| 비활성 | 이유를 한 줄로 제공하며 활성 버튼처럼 보이지 않음. |
| 로딩/저장 중 | 중복 입력 차단, 기존 유효 상태 유지. |
| 빈 상태 | 무엇이 비었고 다음 가능한 행동이 무엇인지 표시. |
| 오류 | 플레이어가 고칠 수 있는 행동과 안전한 돌아가기 제공. |
| 성공 | 새 스냅샷의 실제 결과를 보여준 뒤 입력 해제. |

텍스트는 어떤 지원 크기에서도 컨테이너 밖으로 잘리거나 다음 요소를 덮지 않는다.
한국어 이름과 재료 수량의 최장 예시로 검증한다.

## Tasks

### Phase A - 용어와 흐름 교체

- [ ] 클래스, Weapon A/B, enchantment, charm, mastery를 전투 도구, 액티브,
  고유 특성, 패시브, 재료 등급으로 매핑한다.
- [ ] 메인 흐름을 프로필 -> 연습장/건너뛰기 -> 장비 준비 -> 스테이지 지도로 정리한다.
- [ ] UI 스냅샷과 intent 계약에서 게임 규칙 계산을 제거한다.
- [ ] 기존 화면별 유지/교체/삭제 목록과 데이터 마이그레이션 표시 규칙을 확정한다.

### Phase B - 프로필, 저장, 연습장

- [ ] 로컬 프로필 3개와 Continue 조건을 구현한다.
- [ ] 저장 중/완료/복구/실패 상태를 구현한다.
- [ ] 연습장 완료와 동일 보상 건너뛰기 흐름을 구현한다.
- [ ] 키보드·게임패드 포커스와 삭제/포기 확인을 검증한다.

### Phase C - 장비 준비와 대장간

- [ ] 세 전투 도구, 세 액티브, 네 지원 장비 슬롯의 준비 화면을 구현한다.
- [ ] 도구의 액티브/고유 특성/자원/약점과 재료 등급 수치 비교를 구현한다.
- [ ] 제작, 재제작, 수리, 보급 탭과 모든 상태를 구현한다.
- [ ] 부족 재료, 무료 최소 정비, 도구별 원거리 자원, 마모 상태를 정확히 표시한다.
- [ ] `960x540`, `1280x720`, `1920x1080`에서 재배치와 포커스를 검증한다.

### Phase D - 전투 HUD와 공격 명확성

- [ ] 클래스 상태와 상시 재료 띠를 제거한다.
- [ ] 장착 원거리 자원 하나, 근접/방패 상태, 공명, 제압/전술/정령술을 표시한다.
- [ ] 근접/원거리 표적 미리보기와 실제 판정이 같은 데이터를 쓰게 한다.
- [ ] 활 궤적, 총 장전, 투척날 왕복, 각인판 지면 표식을 각각 캡처 검증한다.
- [ ] 방어/정밀 방어/방어 불가 공격 피드백을 구현한다.
- [ ] 원형/소형/대형/거울 방패의 방어 상태를 각각 캡처 검증한다.
- [ ] 일반 방, 다수 적, 보스, 저체력, 원거리 자원 0, 상태 0 화면을 검증한다.

### Phase E - 필드 아이템과 보상

- [ ] 재료 계열별 실루엣, 등급 표식, 튐, 자석 획득, 안전 회수를 구현한다.
- [ ] 적 보상 정산과 보이는 필드 아이템을 한 트랜잭션으로 연결한다.
- [ ] 회복, 도구별 원거리 보급, 화약, 수리 도구의 획득 가능/불가 상태를 구현한다.
- [ ] 설계도, 기술서, 정령석, 상자, 퀘스트, 보스의 큰 보상 흐름을 구현한다.
- [ ] 중복 신호와 저장 복구에서 보상이 한 번만 표시·적용되는지 검증한다.

### Phase F - 스테이지 지도와 재도전

- [ ] 첫 완료/반복 보상과 놓친 고정 획득처를 분리 표시한다.
- [ ] 재료 계열, 최소 보장, 위험 태그, 예상 시간을 표시한다.
- [ ] 다음 스테이지와 이전 지역 재도전이 저장·준비 흐름과 연결되게 한다.
- [ ] 기본 보급 부족이 거짓 잠금 상태를 만들지 않는지 검증한다.

### Phase G - 선택, 결과, 설정

- [ ] 카드/레벨 선택이 현재 장비에 적용되는 이유와 결과를 보여준다.
- [ ] 액티브, 고유 특성, 정령 동조, 장신구, 런 효과를 같은 목록에 섞지 않는다.
- [ ] 사망/완료 결과에 획득, 상태, 저장 결과를 정리한다.
- [ ] 입력 리매핑과 마지막 입력 장치 아이콘 전환을 새 액션에 맞춘다.
- [ ] 모션 감소, 화면 흔들림, 피해 번쩍임, 진동, 색 외 구분을 검증한다.

### Phase H - 최종 UI 품질 게이트

- [ ] 모든 지원 크기에서 겹침, 잘림, 가로 스크롤, 화면 밖 포커스가 없다.
- [ ] 로딩, 빈 상태, 오류, 비활성, 선택, 성공 상태가 실제 경로에서 도달 가능하다.
- [ ] 작동하지 않는 탭·버튼·설정과 개발용 텍스트가 없다.
- [ ] 전투 HUD가 캐릭터, 적 경고, 이동 경로, 보스 패턴을 가리지 않는다.
- [ ] 새 플레이어가 공격/방어/획득/제작/저장을 설명문 없이 완료한다.
- [ ] 생산 빌드의 실제 장면을 세 화면 크기로 캡처해 육안 검토한다.

## Progress

- [x] 현재 생산 UI, 입력, 필드 아이템, 적 보상, 저장 기반을 코드에서 확인했다.
- [x] 한 영웅·세 전투 도구·세 액티브·패시브 출처·제작 사양과 UI 책임을 분리했다.
- [x] As-Is/To-Be 흐름, 화면 계약, 상태 행렬, 단계별 품질 게이트를 문서화했다.
- [ ] Phase A 이후의 생산 코드와 장면 구현은 아직 시작하지 않았다.

## Verification

각 Phase는 해당 집중 검증과 함께 다음 공통 확인을 실행한다.

- 생산 UI 정적/런타임 validator;
- 키보드와 게임패드 전체 주요 경로;
- `960x540`, `1280x720`, `1920x1080` 실제 캡처;
- 긴 한국어 이름, 최대 수량, 부족 재료, 저장 실패, 원거리 자원 0, 상태 0 fixture;
- HUD와 캐릭터/적/위험 경고의 겹침 검사;
- 포커스 순서, 접근 가능한 이름, 색 외 상태 구분;
- 보상 트랜잭션 중복 적용/중복 표시 검사;
- 최종 빌드 후 production-like 실행과 수동 플레이 검토.

## Risks

- 자동 공격 선택이 정확해도 미리보기가 늦으면 플레이어는 입력이 틀렸다고 느낀다.
- 상태, 원거리 자원, 공명, 세 액티브를 모두 크게 표시하면 기존 HUD 과밀 문제가 반복된다.
- 패시브 네 출처를 상시 슬롯으로 만들면 액티브/패시브 분리가 화면에서 다시 무너진다.
- 적 보상을 물체로 만들 때 정산 시점을 잘못 바꾸면 중복 지급 또는 유실이 생긴다.
- 준비와 대장간을 한 화면에 과도하게 합치면 첫 화면이 다시 설명서가 된다.
- 한국어 표시를 나중으로 미루면 영문 축약 기준 레이아웃이 다시 잘릴 수 있다.
- 기존 3프로필 저장 마이그레이션 오류가 UI 문제처럼 보일 수 있으므로 상태 소유자를
  분리해 진단해야 한다.

## Next Steps

1. `COMBAT_EQUIPMENT_CRAFTING.md`의 공격 선택·상태·도구별 자원 규칙을 대표 시나리오로
   고정하고, 기존 세 프로필 fixture를 보존한다.
2. Phase A에서 UI 스냅샷/intent와 저장 마이그레이션 경계를 먼저 정의한다.
3. 새 캐릭터 선택 화면을 만들기 전에 장비 준비의 데이터 모델과 저해상도 레이아웃
   캡처를 작은 프로토타입으로 검증한다.
4. HUD는 근접/원거리 선택 미리보기와 실제 판정 공유부터 구현한 뒤 상태,
   원거리 자원, 제압/전술/정령술을 추가한다.
5. 일반 필드 재료와 적 보상 한 종류를 끝까지 연결한 후 나머지 계열로 확장한다.
