Use case: stylized-concept
Asset type: Cardborne Space Hangar V2 top-down game structure stamp sprite sheet
Input images: Image 1 is the style reference for Cardborne's flat-color top-down pixel-art palette and rendering; Image 2 is the approved reference for segmented hangar structure shapes and material language. Use only their visual language. Do not reproduce either image's map, floor, UI, actors, props, or overall composition.
Primary request: Create exactly ONE 1024×1024 sprite sheet containing a strict 4-column × 4-row array of 16 isolated structure stamps. The invisible cell boundaries are x=0/256/512/768/1024 and y=0/256/512/768/1024, so every cell is exactly 256×256 pixels. Do not draw the cell boundaries.
Scene/backdrop: The entire 1024×1024 canvas, including every cell and every gap around every structure, is one perfectly uniform solid chroma-key magenta #FF00FF. No variation, texture, floor, lighting, gradient, shadow, border, or grid line may appear in the magenta background.
Composition/framing: Put exactly one complete structure stamp in each cell, centered within that cell, with at least 24 output pixels of uninterrupted #FF00FF clearance between the stamp and all four cell edges. Nothing may touch or cross a cell boundary. No stamp may overlap another. All stamps are seen perfectly top-down orthographic and aligned exactly to the horizontal or vertical canvas axes.
Row-major cell contents, left to right, top to bottom: row 1 = frame_h, frame_v, corner_nw, corner_ne; row 2 = corner_se, corner_sw, rail_h, rail_v; row 3 = service_bay_h, service_bay_v, cover_small, cover_wide; row 4 = bulkhead_h, bulkhead_v, inner_cap_h, inner_cap_v. These identifiers are placement instructions only: NEVER render their names, letters, labels, captions, numbers, legends, or any other text.
Subject design: frame_h and frame_v are straight segmented perimeter-frame stamps in horizontal and vertical orientations; the four corner stamps are distinct matching 90-degree outer-frame elbows oriented northwest, northeast, southeast, and southwest; rail_h and rail_v are slim straight protective rails; service_bay_h and service_bay_v are compact recessed service-bay modules with horizontal and vertical orientation; cover_small is one compact low rectangular cover; cover_wide is one wider low rectangular cover; bulkhead_h and bulkhead_v are heavy reinforced straight bulkhead segments; inner_cap_h and inner_cap_v are short terminating inner-frame cap pieces. Each stamp must read as a single isolated game structure, not as a room, scene, map tile field, or multi-prop vignette.
Style/medium: Flat crisp pixel art with hard pixel edges and consistent 4× logical pixel scaling. Perfectly top-down game-ready silhouettes. Segmented graphite chassis, cool blue-gray metal, cobalt recessed insets, and restrained mint edge lights. Chunky readable construction, sparse purposeful seams, compact local hard-pixel occlusion only. Match the references' Sunken Ceramic Fresco hangar language while simplifying every stamp into a clean reusable structure asset.
Constraints: Exactly 16 stamps; exact 4×4 arrangement; exact row-major ordering; exactly one stamp per cell; exactly 256×256 per invisible cell; at least 24 pixels of magenta clearance on every side; uniform #FF00FF everywhere outside stamp pixels; no object-local color may use #FF00FF; no transparent pixels; no visual labels.
Avoid: any text, glyphs, labels, letters, numbers, icons, border, grid line, divider, panel frame around the sheet, UI, HUD, character, vehicle, actor, enemy, pickup, loose prop, floor field, room, full map, map composition, overlapping, merged cells, perspective, isometric view, tilted objects, soft shadow, cast shadow, contact shadow on the background, glow, bloom, fog, reflection, gradient, antialiasing, subpixel smoothing, watermark, signature, collage framing, multiple sheets, or mockup presentation. Output one clean square sheet only.
