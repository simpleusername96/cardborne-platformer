# .agent/Implement.md

## Execution Rule
- The active ExecPlan named in `Documentation.md` is the execution source of truth.
- Consult root `AGENTS.md` plus the nearest local `AGENTS.md` for durable constraints and placement rules.
- Use `docs/README.md` to resolve product and visual authority before coding.
- Treat research and references as evidence, never as implementation instructions.

## Discipline
- Keep changes scoped to the active milestone.
- Validate after each milestone with Godot and focused top-down gameplay checks.
- Extend a visible playable path after the first combat contract; do not grow generic foundations without an accepted player-facing use.
- Record direct play evidence before widening content; the pivot is invalid if the combat proof is not more enjoyable than the retired baseline.
- Record meaningful status in `Documentation.md`.
- Do not infer a long-term structure contract from the current folder layout alone.
