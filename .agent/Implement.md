# .agent/Implement.md

## Execution Rule
- `Plan.md` or the active ExecPlan is the source of truth.
- Consult root `AGENTS.md` plus the nearest local `AGENTS.md` for durable constraints and placement rules.
- Use the PRD as product scope; do not promote illustrative future expansion items into MVP implementation unless they are in the MVP requirements.

## Discipline
- Keep changes scoped to the active milestone.
- Validate after each milestone with Godot and focused gameplay checks.
- Record meaningful status in `Documentation.md`.
- Do not infer a long-term structure contract from the current folder layout alone.
